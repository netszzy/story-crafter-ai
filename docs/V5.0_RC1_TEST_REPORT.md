# V5.0-rc1 全流程测试验证报告

**生成时间**: 2026-05-06
**更新**: 2026-05-07 V5.0-UX 收口复测
**测试范围**: 数据层 → 管线层 → WebUI 组件层 → 集成与边界 → WebUI 启动烟测

---

## 总览

| 指标 | 数值 |
|---|---|
| 总测试数 | 413 |
| 通过 | 413 |
| 失败 | 0 |
| 核心模块导入 | 11/11 OK |
| 新增测试文件 | 3 个 |
| 新增测试用例 | 95 个 |

---

## 测试文件清单

| 文件 | 测试数 | 说明 |
|---|---|---|
| `test_literary_critic.py` | (原有) | 文学批评 schema 与 mock 回退 |
| `test_quality_diagnostics.py` | (原有) | 质量诊断覆盖度与分数计算 |
| `test_dramatic_arc.py` | (原有) | 戏剧弧光诊断和趋势 |
| `test_style_court.py` | (原有) | 风格法庭裁决逻辑 |
| `test_editor_memo.py` | (原有) | 编辑备忘录和 revision prompt |
| `test_voice_diagnostics.py` | (原有) | 角色声音对比和文风提示 |
| `test_webui_infra.py` | (原有) | WebUI 键盘、导航、状态、卷轴 |
| `test_long_outline.py` | (原有) | 长文本大纲解析 |
| `test_style_profiles.py` | (原有) | 风格档案加载和合并 |
| `test_novel_schemas.py` | (原有) | 全局 schema 序列化验证 |
| **`test_v5_rc1_data_layer.py`** | **38** | **V5.0-rc1 数据层专项（新增）** |
| **`test_v5_rc1_pipeline.py`** | **27** | **V5.0-rc1 管线层专项（新增）** |
| **`test_v5_rc1_integration.py`** | **27** | **V5.0-rc1 集成与边界专项（新增）** |

2026-05-07 追加覆盖：

| 文件 | 新增覆盖 |
|---|---|
| `test_webui_infra.py` | inline diff 差异块逐块采用/保留、后台任务 result 回填 |
| `test_pipeline.py` | 删除第 001 章后，新建章纲不再依赖 `第001章.md` 模板 |

---

## 新增测试覆盖矩阵

### 阶段 2: 数据层 (38 tests)

| 测试类 | 测试数 | 覆盖点 |
|---|---|---|
| `DiagnosticReservationSchemaTests` | 8 | action 默认值/protect/adopt/rebut、JSON 往返、向后兼容（旧记录缺 action→protect）、非法 action 拒绝、finding_key 默认、created_at 自动生成 |
| `ProjectHealthSnapshotSchemaTests` | 5 | 默认值、JSON 往返、ChapterHealthSnapshot 字段 |
| `WriteWriterOverrideTests` | 9 | keyword-only args 写入、三段读回、apply 三态分流（adopt 不退分/protect 退分/rebut 退分）、finding_key 去重、rejected_advice 去重、分数退款验证 |
| `ActiveFindingsFilterTests` | 4 | style_court 和 quality_diagnostics 的双重过滤（accepted_by_writer+rebutted_by_writer）、空报告、无 findings 键 |
| `EditorMemoReservationsMergeTests` | 3 | adopt 条目跳过、旧记录默认 protect、空 advice 去重 |
| `ProjectHealthComputeTests` | 6 | 空项目、质量分聚合、文学密度归一化、风格一致度计算、弱章识别、损坏 JSON 降级 |
| `ScrollHealthThreeDimensionTests` | 8 | 颜色渐变、空分处理、clamp 边界、三维独立分数、三维弱章识别、None 分数忽略 |

### 阶段 3: 管线层 (27 tests)

| 测试类 | 测试数 | 覆盖点 |
|---|---|---|
| `QualityDiagnosticsPipelineTests` | 5 | 完整写-读-应用周期、rebut 字段记录、adopt 不退分、多次覆盖最终生效、未匹配 override 无影响 |
| `StyleCourtPipelineTests` | 8 | hard/soft issue 判定、active findings 过滤、保护模式+不可量化→contested、hard issue 始终 confirmed、无 LiteraryView 时不做保护、contested_to_reservations、写-读往返 |
| `EditorMemoPipelineTests` | 5 | adopt 排除、去重、memo_to_revision_prompt 含 reservations、_finding_contested_by_style_court、空 memo 短路径 |
| `VoiceDiagnosticsPipelineTests` | 3 | VoiceFingerprint 模型往返、默认值、mock 模式 analyze_character_voices |
| `LiteraryCriticPipelineTests` | 3 | LiteraryView 含 MemorableMoment 的 model、write-read 往返、空 moments |
| `DramaticArcPipelineTests` | 2 | DramaticDiagnostics 正确的字段名映射、write-read 往返 |
| `CrossModuleConsistencyTests` | 1 | style_court 与 editor_memo 的 `_is_mock_caveat` 逻辑一致性 |

### 阶段 6-7: 集成与边界 (27 tests)

| 测试类 | 测试数 | 覆盖点 |
|---|---|---|
| `CrossModuleDataFlowTests` | 5 | 质量→风格法庭→备忘录完整链、override 影响下游 must_fix、adopt 保持活跃不被过滤、项目健康多章聚合、scroll_health 三维切换 |
| `BoundaryConditionTests` | 15 | 章号 0/999、空报告 apply、无 findings 键、findings 非 list、空 advice override、Unicode 理由、颜色极值 clamp、空项目健康、缺失文学/声音文件降级、读不存在的文件、None→空 reservations、空 top_3 short-circuit、5次覆盖去重 |
| `NovelPipelineIntegrationTests` | 7 | pipeline 导入不崩溃、CJK 字数统计、可操作审计问题检测、伏笔 ID 提取、mock 环境设置、纯标点字数 |

---

## 发现的 Bug 与问题

### BUG-1: `write_writer_override` 不拒绝空 `rejected_advice` (Low Severity) — ✅ 已修复

- **位置**: [`quality_diagnostics.py:381`](D:\cc\novel\quality_diagnostics.py#L381)
- **修复**: 函数开头添加 `if not rejected_advice.strip(): return` 守卫
- **测试**: `test_override_with_empty_rejected_advice`

### BUG-2: `apply_writer_overrides` 对空报告添加 `writer_overrides` 键 (Low Severity) — ✅ 已修复

- **位置**: [`quality_diagnostics.py:442`](D:\cc\novel\quality_diagnostics.py#L442)
- **修复**: 当 `findings` 为空列表时提前返回，不修改原 report
- **测试**: `test_empty_report_apply_overrides`

### BUG-3: `editor_memo._is_mock_caveat` 与 `style_court._is_mock_caveat` 代码重复 (Low Severity) — ✅ 已修复

- **位置**: [`style_court.py:275`](D:\cc\novel\style_court.py#L275) 和 [`editor_memo.py:594`](D:\cc\novel\editor_memo.py#L594)
- **修复**: `editor_memo` 改为从 `style_court` 导入该函数，删除重复定义
- **测试**: `test_style_court_and_editor_memo_same_mock_caveat`

### BUG-4: `_active_findings` 签名不一致 (Medium Severity) — ✅ 已修复

- **位置**: [`quality_diagnostics.py:524`](D:\cc\novel\quality_diagnostics.py#L524) vs [`style_court.py:208`](D:\cc\novel\style_court.py#L208)
- **修复**: `style_court._active_findings` 委托 `quality_diagnostics._active_findings` 执行核心过滤，自身仅负责从 report dict 中提取 findings 列表。过滤规则（排除 `accepted_by_writer` / `rebutted_by_writer`）统一在 quality_diagnostics 维护
- **测试**: `test_quality_active_findings_filters_both` / `test_style_court_filters_both_accepted_and_rebutted`

---

## 验证结论

### 向后兼容性

- `DiagnosticReservation` 旧格式（无 `action` 字段）反序列化正确默认 `"protect"`
- `apply_writer_overrides` 对旧格式 `overrides` dict（含 `"overrides"` 键）正确处理
- `_merge_reservations` 对旧记录（无 `action` 字段）正确保留并视为 protect

### 三态裁决（T4.2 核心功能）

| 裁决 | findings 状态 | 分数退款 | must_fix | reservations |
|---|---|---|---|---|
| adopt（采纳） | 保持原 level，标记 `writer_action="adopt"` | 不退 | 仍进入 | 不进入 |
| protect（保护） | 标记 `accepted_by_writer`，退分 | 退款 | 排除 | 进入 |
| rebut（反驳） | 标记 `rebutted_by_writer`，退分 | 退款 | 排除 | 进入 |

所有三态行为已通过 9 个 pipeline + 9 个 data-layer 测试验证。

### 三维项目健康（T4.4 核心功能）

- `compute_project_health` 正确计算工程稳健度（质量分 mean）
- 文学密度归一化正确（memorable_moments / 5 * 100）
- 风格一致度归一化正确（100 - flagged_pairs * 20）
- 弱章识别每个维度独立
- 趋势计算合理（最近 3 章 vs 前 3 章，差异阈 5 分）

### 附录回归验证

- **B.1（第001章 59分误判）**：protect "冲突信号偏弱" → `accepted_by_writer`，退分 10，不再进入 must_fix ✓
- **B.2（郁时谌双层人格）**：rebut "角色主动性偏弱" → `rebutted_by_writer`，退分 8，不再进入 must_fix ✓
- **A.2（不可量化保护）**：三指标独立显示，无单一总分合成 ✓

---

## 未覆盖项

以下仍需真实浏览器手动验收：

1. **WebUI 页面渲染**：写作页工具栏风格指示器、诊断抽屉四标签页、裁决面板三态按钮
2. **Streamlit session state 交互**：按钮点击 → popover → 确认 → 写入 → rerun 完整流程
3. **块级 inline diff 浏览器点击流**：逐块采用/保留 → 全部裁决 → 写入已裁决版本
4. **后台任务真实供应商调用**：取消按钮只能尽力请求取消，不能强行中断已发出的 HTTP 调用

手动验收建议执行：
```bash
streamlit run webui.py
# 验证：写作页 → 选择第N章 → 展开诊断抽屉 → 各标签页可见
# 验证：裁决按钮 → popover → 输入理由 → 确认后状态更新
# 验证：仪表板 → 三指标健康卡片 → 数值与趋势显示
# 验证：生成 inline diff → 逐块采用/保留 → 写入正文
```

---

## 测试运行记录

```
$ cd D:\cc\novel
$ python -m unittest discover -s tests -v
Ran 410 tests in 14.470s
OK

$ python -c "import novel_schemas, quality_diagnostics, style_court, ..."
Imports OK: 11/11

$ python -m unittest discover -s tests -v
Ran 413 tests in 14.516s
OK

$ streamlit run webui.py --server.headless=true --server.port=8509
STREAMLIT_SMOKE_OK http://localhost:8509
```

---

*报告由自动化测试流程生成。所有新增测试文件位于 `D:\cc\novel\tests\test_v5_rc1_*.py`。*
