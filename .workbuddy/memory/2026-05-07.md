# 2026-05-07 工作日志

## V5.0 原始设计稿对照与 UX 收口

- 对照 `docs/V5.0_UPGRADE_PLAN.md`、`docs/V5.0_DESIGN_SOUL_APPENDIX.md` 以及桌面两份初始设计稿完成核验。
- 删除测试用第 001 章：相关章节资产移动到 `99_回收站/第001章_20260507_013144/`；确认 `01_大纲/章纲`、`02_正文`、`04_审核日志` 无 `第001章*` 残留。
- 修复删除第 001 章后的模板依赖：新建章纲不再依赖 `01_大纲/章纲/第001章.md`。
- 清理 WebUI 旧 UX 痕迹：当时阶段顶层导航收敛为 `今天`、`全书`、`笔记`、`设置`；后续已继续调整为 `写作`、`故事圣经`、`规划`、`AI任务`、`设置`。同时移除页面代码里的 emoji 状态和非 palette 硬编码颜色；辅助稿入口收拢为 inline diff。

## 块级 inline diff

- `webui_infra/pages/writing.py` 新增差异块生成与合成逻辑：每个变更块可单独“采用此块”或“保留原文”。
- 全部差异块裁决后才允许写入；若源文件在生成 diff 后变化，会阻止写入，避免覆盖新内容。
- 写作 AI、单段润色、备忘录改稿和戏剧建议改稿都会生成可逐块裁决的 inline diff。

## 后台任务化

- `webui_infra/background_jobs.py` 的 `BackgroundJob` 增加 `on_success` / `on_error`，保留 result/error，供 Streamlit rerun 时回填。
- `webui.py` 新增 `_start_llm_background_job()`，主要 LLM 长任务不再走同步 `st.status` 等待框。
- 已后台化范围：写作 AI、单段润色、备忘录/戏剧建议改稿、智能推荐动作、主流水线、逐步审计、诊断驱动改稿、定稿记忆更新、场景计划/生成/审稿/对比、世界观/大纲/启动包 AI 生成与改稿。
- 取消按钮是尽力请求；不能强制中断供应商已经接收的 HTTP 调用。

## V5.0-UX 继续收口

- 顶层导航已调整为 `写作`、`故事圣经`、`规划`、`AI任务`、`设置`；旧 `今天`、`全书`、`笔记` 等深链仍通过 alias 映射。
- 写作页新增 `AI 自动推进当前章`：按 `workflow_advisor.chapter_flow()` 依次执行章纲生成/采纳、章纲审查、章纲改稿、任务卡、场景、草稿、审计、检查、修订和记忆更新。
- AI 自动推进断点写入 `05_项目管理/AI推进断点/第NNN章.json`；模型中断时会尽量从已落盘章纲草案恢复，下次点击按当前文件状态续接。
- 后台任务完成后写入站内信，侧边栏和 `AI任务 / 后台消息` 可查看；前台只 toast 提醒，不强制刷新正在编辑的页面。
- 设置页只显示当前写/策/改/审路由实际启用的供应商，未启用模型项隐藏；API Key、模型、URL、超时和本地检索参数改为多列紧凑排布。
- 旧测试第 001 章已在 2026-05-07 移入回收站，但用户后续可以创建合法第 001 章；不要只按章号判断测试污染。

## 验收

- `python -m py_compile webui.py webui_infra/pages/writing.py webui_infra/background_jobs.py` 通过。
- `python -m unittest discover -s tests -v`：413 项通过。
- Streamlit 烟测：`http://localhost:8509` 返回 200。
- 设置页与自动推进小步修复后跑过 `python -m py_compile webui.py` 和 `python -m unittest tests.test_pipeline.WebUIHelperTests tests.test_webui_infra -v`，40 项通过。
